<?php 
	
	// limitar as extensões ? ( sim ou não )
	$limitar_ext = "sim";

	// extensões autorizadas
	$extensoes_validas = array(".jpg",".jpeg",".png");

	// caminho absoluto onde os arquivos serão armazenados
	$caminho_absoluto = "../media/images/produtos/";

	// limitar o tamanho do arquivo ? ( sim ou não )
	$limitar_tamanho = "não";

	// tamanho limite do arquivo em bytes
	$tamanho_bytes = "2000000";

	// se o arquivo ja existir, indica se ele deve ser sobrescrevido ( sim ou não )
	$sobrescrever = "nao";


?>